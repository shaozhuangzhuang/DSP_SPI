# 命令行编译指南

**项目名称**: TMS320F28377D Prj03  
**编译配置**: CPU1_FLASH_STANDALONE  
**CCS版本**: 12.1.0  
**编译器**: TI C2000 v22.6.0.LTS  
**日期**: 2025-10-21

---

## ✅ 编译成功记录

### 编译结果

```
┌─────────────────────────────────────┐
│   🏆 命令行编译成功 🏆             │
├─────────────────────────────────────┤
│  ✅ 编译错误：    0 个              │
│  ✅ 链接错误：    0 个              │
│  ✅ 编译警告：    0 个              │
│  ✅ 生成文件：    Prj03.out         │
│  ✅ 文件大小：    603.3 KB          │
│  ✅ 看门狗驱动：  已集成 ✅         │
└─────────────────────────────────────┘
```

### 看门狗符号验证

从 `Prj03.map` 文件中找到：
```
_Drv_Watchdog_Init    @ 0x0008485b
_Drv_Watchdog_Service @ 0x00084870
```

✅ **看门狗驱动已成功编译并链接到项目中！**

---

## 🔧 命令行编译步骤

### 环境要求

- CCS安装路径：`C:\ti\ccs1210\ccs`
- 编译器路径：`C:\ti\ccs1210\ccs\tools\compiler\ti-cgt-c2000_22.6.0.LTS`
- gmake工具：`C:\ti\ccs1210\ccs\utils\bin\gmake.exe`

---

### 编译命令

#### 1. 清理项目（可选）

```powershell
cd C:\Users\szz\Desktop\F28377D_Prj\Prj03\CPU1_FLASH_STANDALONE
C:\ti\ccs1210\ccs\utils\bin\gmake.exe clean
```

#### 2. 完整编译

```powershell
cd C:\Users\szz\Desktop\F28377D_Prj\Prj03\CPU1_FLASH_STANDALONE
C:\ti\ccs1210\ccs\utils\bin\gmake.exe all -j4
```

参数说明：
- `all` - 编译所有文件
- `-j4` - 使用4个并行任务（加速编译）

#### 3. 增量编译

```powershell
C:\ti\ccs1210\ccs\utils\bin\gmake.exe
```

---

### 简化命令（创建批处理文件）

创建 `build.bat`：

```batch
@echo off
cd /d %~dp0\CPU1_FLASH_STANDALONE
C:\ti\ccs1210\ccs\utils\bin\gmake.exe all -j4
echo.
echo ====================================
echo 编译完成！
echo ====================================
pause
```

使用方法：双击 `build.bat` 即可编译

---

## 📊 编译输出分析

### 编译阶段

```
1. 清理阶段（clean）
   删除所有 .obj, .d_raw 文件

2. 编译阶段（compile）
   - 编译源文件：*.c, *.asm
   - 生成目标文件：*.obj
   - 生成依赖文件：*.d_raw

3. 链接阶段（link）
   - 链接所有.obj文件
   - 链接TI库文件（.lib）
   - 生成可执行文件：Prj03.out
   - 生成map文件：Prj03.map
```

### 看门狗模块编译过程

```
Building file: "../Drivers/drv_watchdog.c"
  ↓
Invoking: C2000 Compiler
  ↓
Finished building: "../Drivers/drv_watchdog.c"
  ↓
Invoking: C2000 Linker
  ↓
Finished building target: "Prj03.out" ✅
```

---

## 🚀 CCS eclipsec 命令行编译（备选方案）

### 注意事项

⚠️ **Workspace锁定问题**：如果CCS IDE已打开工程，workspace会被锁定，导致命令行编译失败。

**解决方案**：
1. 关闭CCS IDE
2. 或使用不同的workspace路径

### 命令格式

```powershell
# 完整编译
C:\ti\ccs1210\ccs\eclipse\eclipsec.exe `
  -noSplash `
  -data "C:\Users\szz\workspace_cli" `
  -application com.ti.ccstudio.apps.buildProject `
  -ccs.projects Prj03 `
  -ccs.configuration CPU1_FLASH_STANDALONE `
  -ccs.buildType full

# 增量编译
C:\ti\ccs1210\ccs\eclipse\eclipsec.exe `
  -noSplash `
  -data "C:\Users\szz\workspace_cli" `
  -application com.ti.ccstudio.apps.buildProject `
  -ccs.projects Prj03 `
  -ccs.configuration CPU1_FLASH_STANDALONE `
  -ccs.buildType incremental

# 清理
C:\ti\ccs1210\ccs\eclipse\eclipsec.exe `
  -noSplash `
  -data "C:\Users\szz\workspace_cli" `
  -application com.ti.ccstudio.apps.buildProject `
  -ccs.projects Prj03 `
  -ccs.configuration CPU1_FLASH_STANDALONE `
  -ccs.buildType clean
```

---

## 🎯 推荐编译方案

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| **gmake直接编译** | 速度快、不依赖workspace | 需要已有makefile | ⭐⭐⭐⭐⭐ |
| **eclipsec命令行** | 功能完整、支持项目管理 | 可能遇到workspace锁定 | ⭐⭐⭐ |
| **CCS IDE编译** | 图形化界面、调试方便 | 不适合自动化 | ⭐⭐⭐⭐ |

**推荐**：日常开发使用 **gmake**，CI/CD集成使用 **eclipsec**

---

## 📝 编译检查清单

编译完成后，请检查：

- [ ] `Prj03.out` 文件已生成
- [ ] `Prj03.map` 文件已生成
- [ ] map文件中包含 `_Drv_Watchdog_Init` 和 `_Drv_Watchdog_Service`
- [ ] 无编译错误（error）
- [ ] 无编译警告（warning）
- [ ] 文件大小约603KB（正常范围）

---

## 🔍 故障排除

### 问题1：找不到头文件

**错误**：`fatal error: file not found`

**解决**：检查Include路径是否正确配置

### 问题2：链接错误

**错误**：`undefined symbol`

**解决**：
1. 检查Makefile中的ORDERED_OBJS是否包含所有.obj
2. 检查库文件路径是否正确

### 问题3：gmake命令未找到

**错误**：`gmake不是内部或外部命令`

**解决**：使用完整路径或添加到PATH环境变量

---

## 📊 编译统计

| 项目 | 数量/大小 |
|------|----------|
| 源文件总数 | ~50个 |
| 编译时间 | ~30秒（并行编译） |
| 目标文件大小 | ~2MB |
| 可执行文件大小 | 603.3 KB |
| 看门狗代码大小 | ~1KB |

---

## ✅ 编译成功标志

看到以下输出即为成功：

```
'Building file: "../Drivers/drv_watchdog.c"'
'Finished building: "../Drivers/drv_watchdog.c"'
'Invoking: C2000 Linker'
'Finished building target: "Prj03.out"'
```

没有 `error #` 或 `warning #` 输出。

---

## 📅 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2025-10-21 | 初始版本，验证看门狗模块编译成功 |

